git add .           //실무에서 별로 좋지않음(변경한 모든파일을 올려버림)
git commit -m "제목"
git push 

//git push할때 branch가 안올라 가 있으면  
//git push --set-upstream origin <branch>로 서버쪽에 branch를 생성하고 올려야한다

bug->issue, blame -> annotate

 
git blame <파일명>
git annotate <파일명>


revert
commit -> add